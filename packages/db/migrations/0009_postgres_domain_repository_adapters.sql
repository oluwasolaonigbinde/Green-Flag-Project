-- Slice 8.6: PostgreSQL domain repository adapter support.
-- Runtime persistence now uses the relational tables introduced by Slices 3-8.
-- No additional schema is needed in this migration.

SELECT 1;

-- migrate:down

SELECT 1;
