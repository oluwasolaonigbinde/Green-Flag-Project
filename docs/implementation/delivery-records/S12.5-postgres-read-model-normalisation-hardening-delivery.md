# S12.5 PostgreSQL Read Model Normalisation Hardening Delivery

## Status

- Slice: 12.5
- Status: DONE_FULL
- Closed: 2026-05-06
- Contract: `docs/implementation/slice-contracts/S12.5-postgres-read-model-normalisation-hardening.md`

## Delivered

- Added `assessment_score_entries` as a normalized child table for S11 score entries.
- Added targeted indexes for application sections/fields, document queue state, upload sessions, invoice/payment state, assessor capacity/availability, assessment visits, judge assessment threshold state, decision publication, and park award cache.
- Updated PostgreSQL domain adapters to write application sections and field values into relational tables.
- Updated document upload adapter writes to populate `document_upload_chunks`.
- Updated assessor adapter writes to populate preferences, availability windows, and capacity declarations.
- Updated assessment adapter writes to populate normalized score entries.
- Preserved existing DTO hydration during the transition to relational read models.
- Added Docker-backed DB integration coverage verifying normalized rows and payload compatibility are written together.

## Deferred

- Full removal of the legacy JSON compatibility envelope.
- Load-driven query-plan tuning.
- S13 notifications, messaging, jobs, exports, and reminders.
- S14 UAT/security/performance hardening.

## Checks

- `corepack pnpm install --frozen-lockfile`: PASS
- `corepack pnpm contracts:check`: PASS
- `corepack pnpm openapi:check`: PASS
- `corepack pnpm db:migrate:check`: PASS
- `corepack pnpm db:seed:check`: PASS
- `corepack pnpm lint`: PASS
- `corepack pnpm test`: PASS
- `corepack pnpm build`: PASS
- `corepack pnpm typecheck`: PASS after rerun with longer timeout
- `corepack pnpm db:integration:test`: PASS

## Notes

- Build/typecheck still emit the existing non-blocking Next.js ESLint plugin warning.
