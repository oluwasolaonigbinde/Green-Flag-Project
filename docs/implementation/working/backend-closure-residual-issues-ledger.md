# Backend Closure Residual Issues Ledger

Date: 2026-05-08  
Scope: focused read-only backend closure checkpoint after Passes 1B, 1C, 2A, 2B, 2C, and 2D. No live AWS staging/production environment exists yet; "production-like runtime" here means local execution under production/staging-style configuration against PostgreSQL.

Closeout Pass B update, 2026-05-10: retry/idempotency, production-like runtime guard proof, GitHub CI, and clean migration apply readiness items from this ledger were closed as PASS WITH CAVEATS. Provider-backed actions remain disabled until real staging/provider configuration is supplied.

## 1. Executive verdict

Needs security/redaction fix before continuing.

The DB-first persistence remediation is substantially closed for the named backend command families. Production-like app wiring now creates PostgreSQL repositories for registration/applicant/document/payment, allocation/assessor, assessment/visits/evidence/submit, communications/messages/jobs/exports/reminders, and results/decisions/publication/public-map events (`apps/api/src/index.ts:9-16`, `apps/api/src/postgres-runtime.ts:218-226`). `buildApp({ productionLike: true })` rejects mounted mutable stores without the corresponding DB-first repository (`apps/api/src/app.ts:96-117`), and converted route handlers branch to repositories before legacy Map/flush paths.

The remaining finite blockers before a technically defensible AWS staging/UAT backend are not a legacy-schema rewrite or episode-model challenge. They are public/applicant DTO exposure, admin/RBAC query scope hardening, lower-env provider isolation, production-like config verification, and retry/idempotency coverage for provider-adjacent shells.

Pass 1C report status: no `docs/implementation/working/pass-1c*` report was present in this checkout.

## 2. What is now closed

- DB-first production-like route closure is confirmed for registration, applicant application, document upload metadata, submission, PO/payment/manual payment, allocation, assessor profile/capacity, assessment visit/score/evidence/submit, communications/message/job/export shells, and result hold/publish/withdraw/public-map event paths.
- Converted production-like routes avoid canonical Map mutation and whole-store flushes. Legacy `withTransaction`/`flush*Store` paths still exist in `apps/api/src/postgres-domain-stores/transactions.ts:76-137`, but route branches use repositories first.
- Audit coverage for communications dispatch-stub and renewal reminders was corrected after Pass 2C; both now append audit inside `UnitOfWork` (`apps/api/src/postgres-domain-stores/communications-repository.ts:333-343`, `apps/api/src/postgres-domain-stores/communications-repository.ts:499-510`).
- Mystery allocation contact reveal is now episode-type gated: reveal requires `FULL_ASSESSMENT` and all assignments accepted (`apps/api/src/postgres-domain-stores/allocation-repository.ts:228`).
- Mystery assessment location/evidence suppression is now episode-aware (`apps/api/src/postgres-domain-stores/assessment-repository.ts:378`, `apps/api/src/postgres-domain-stores/assessment-repository.ts:497`).
- Admin-created Mystery messages are suppressed in the DB-first path and applicant message listing filters Mystery/suppressed rows (`apps/api/src/postgres-domain-stores/communications-repository.ts:397-432`, `apps/api/src/postgres-domain-stores/communications-repository.ts:353-372`).
- Applicant result projection is safe: it returns episode, park, status, and safe display label only (`apps/api/src/postgres-domain-stores/results-repository.ts:626-644`).
- Public map event payloads are provider-neutral and minimal; result publication writes only park id, display label, and publication state into map event payloads (`apps/api/src/postgres-domain-stores/results-repository.ts:538-543`, `packages/contracts/src/schemas.ts:1239-1251`).
- Reported verification improved after passes: `db:integration:test` exists (`package.json:15`) and Pass 2C/2D reports record passing DB integration runs.

## 3. Residual issue ledger

| ID | Title | Category | Severity | Evidence with file paths/line numbers where possible | Why it matters | Recommended fix | Must fix before AWS staging | Safe to defer beyond staging |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R-001 | Public/applicant document DTOs expose storage internals | backend fix | high | `documentAssetSchema` includes `storageProvider` and `storageKey` (`packages/contracts/src/schemas.ts:506-517`); document upload completion returns `documentAssetSchema` (`packages/contracts/src/schemas.ts:597-600`); DB mapping returns provider/key (`apps/api/src/postgres-domain-stores/applicant-repository.ts:146-147`). | Applicant/org-facing document APIs expose storage implementation identifiers and object keys. This is not safe for staging/UAT with real storage-shaped data. | Split applicant/org document DTOs from internal/admin document metadata. Return document id, filename, scan/status/version, and signed-access availability only; keep provider/key internal. | Yes | No |
| R-002 | Assessment evidence DTO exposes storage keys and raw assessment data on judge/admin surfaces | backend fix | medium | `assessmentEvidenceSchema` exposes `storageProvider`/`storageKey` (`packages/contracts/src/schemas.ts:1137-1144`); `judgeAssessmentSchema` exposes `assignmentId`, `rawScoreTotal`, and evidence (`packages/contracts/src/schemas.ts:1148-1162`); repository maps evidence storage fields (`apps/api/src/postgres-domain-stores/assessment-repository.ts:221-229`). | This is acceptable only for internal judge/admin APIs. If reused by applicant/org/public surfaces it leaks storage and scoring internals. | Mark/split judge/admin-only DTOs and add forbidden-field tests for applicant/org/public assessment projections. | Conditional | Conditional |
| R-003 | Admin/internal result DTOs expose raw scores, internal notes, and artifact storage keys | backend fix | medium | `decisionResultSchema` includes `rawScoreTotal` and `internalNotes` (`packages/contracts/src/schemas.ts:1198-1210`); `resultArtifactSchema` includes `storageProvider`/`storageKey` (`packages/contracts/src/schemas.ts:1218-1224`); repository maps these for admin detail (`apps/api/src/postgres-domain-stores/results-repository.ts:128-146`). | Current applicant result projection is safe, but shared/admin DTOs are unsafe for future applicant/public reuse. | Keep result detail DTOs explicitly admin/internal or split applicant/public contract types; add leak tests around result/certificate routes. | Conditional | Conditional |
| R-004 | Applicant message DTO exposes hidden thread/message metadata for non-Mystery threads | backend fix | medium | Shared message schema includes `participantActorIds`, `visibleToApplicant`, and `senderActorId` (`packages/contracts/src/schemas.ts:1344-1359`); applicant listing returns the shared shape after DB filtering (`apps/api/src/postgres-domain-stores/communications-repository.ts:353-372`). | Mystery rows are filtered, but applicant/org-facing non-Mystery messages still expose operational actor/visibility metadata. | Create applicant/org message projections without participant actor ids, sender actor ids, or visibility flags; keep the current schema for admin/internal. | Yes | No |
| R-005 | Admin communication/job/export listings are role-wide, not resource-scoped | backend fix | high | `requireAdmin` admits `SUPER_ADMIN`, `KBT_ADMIN`, and `FINANCE_ADMIN` (`apps/api/src/postgres-domain-stores/communications-repository.ts:85-88`); list endpoints select all rows without resource filtering (`apps/api/src/postgres-domain-stores/communications-repository.ts:292-303`, `452-460`, `526-544`, `588-593`). | Finance or country/org-scoped admins can see all messages, notifications, jobs, and export records if they hold an admin role. | Add ownership joins and `canAccessResource` filters to list queries; narrow Finance Admin to payment/finance export scopes. | Yes | No |
| R-006 | Session model uses primary role plus all active scopes | backend fix | high | Primary role is chosen by scope priority (`apps/api/src/auth.ts:63-72`), then all active scopes are copied into the actor context (`apps/api/src/auth.ts:141-142`); `canAccessResource` authorizes against any scope (`apps/api/src/authorization.ts:12-21`). | A user with a powerful primary role and unrelated broad scope can receive cross-role/cross-resource access that no single assignment granted. | Authorize against role-assignment tuples, not primary role plus merged scopes. Add tests for mixed finance/admin/applicant roles across two countries/orgs/parks. | Yes | No |
| R-007 | Read-only viewer/no-mutation behavior is not evidenced across commands | test fix | medium | Mutating repositories check role families directly, e.g. admin/finance/judge checks in `apps/api/src/authorization.ts:24-48`, `apps/api/src/postgres-domain-stores/assessment-repository.ts:96-98`, and communications `requireAdmin`; no focused pass report proves a viewer mutation-denial matrix. | UAT often introduces read-only operational users. Without denial coverage, future role additions can mutate by accident. | Add read-only/viewer role fixtures and negative mutation tests across applicant, admin, allocation, assessment, communications, results, exports, and jobs. | Conditional | Conditional |
| R-008 | Document signed access/download is not audited | backend fix | medium | Signed document access route returns a lower-env URL (`apps/api/src/applicant/routes.ts:572-612`); repository `getDocument` is a read only (`apps/api/src/postgres-domain-stores/applicant-repository.ts:856-863`); no audit append is present for access/download. | Document access can be sensitive, especially Mystery or management plan documents. Production-like access should leave an audit trail even if it is a read-side signed URL command. | Add a `DOCUMENT_ACCESS_REQUESTED` audit event for signed access generation, with redacted metadata and rollback-free append semantics appropriate for read-command auditing. | Conditional | Conditional |
| R-009 | Upload session idempotency still lacks a collision/replay path | backend fix | medium | Pass 1B noted unique index reliance without repository conflict/reload handling; current create path inserts `idempotency_key` (`apps/api/src/postgres-domain-stores/applicant-repository.ts:620-660`) but has no `ON CONFLICT`/catch-and-reload for same-key concurrent replay. | Concurrent retries can return a raw database uniqueness error instead of an idempotent response. | Add `ON CONFLICT` or catch unique violation and reload the existing upload session for same application/idempotency key. | Yes | No |
| R-010 | Result publish idempotency is audit-key based, not backed by side-effect uniqueness for map/artifact rows | backend fix | medium | Publish replay checks `audit_events` by idempotency key (`apps/api/src/postgres-domain-stores/results-repository.ts:233-237`, `490-500`); publication inserts new artifact and public-map event rows (`apps/api/src/postgres-domain-stores/results-repository.ts:513-543`). | Current lock/status/version tests reduce double-publish risk, but artifact/map idempotency depends on audit lookup and decision state rather than unique side-effect keys. | Add unique/idempotent constraints or deterministic side-effect keys for publish artifact/map rows; keep double-publish regression tests. | Conditional | Conditional |
| R-011 | Notification/job/export shells can duplicate work on retries | backend fix | medium | Renewal reminders always create new notification/job ids (`apps/api/src/postgres-domain-stores/communications-repository.ts:463-510`); exports always create a new export job (`apps/api/src/postgres-domain-stores/communications-repository.ts:550-584`); idempotency key is audit metadata only. | Retried UI/API calls can queue duplicate reminders or export jobs. Before real provider dispatch, this is manageable; before AWS staging/UAT it should be predictable. | Apply idempotency-key lookup/unique indexes for renewal run and export creation; document allowed duplicate semantics where intentional. | Conditional | Conditional |
| R-012 | Payment/manual/provider replay hardening is incomplete | backend fix | medium | Manual mark-paid and overrides audit and mutate state (`apps/api/src/postgres-domain-stores/applicant-repository.ts:1037-1072`, `1087-1134`), but there is no provider webhook/event replay model yet; system state records provider automation not implemented (`docs/implementation/system_state.md:84-86`). | Manual payment is usable for lower-env/UAT, but online provider replay/dedup is not ready. | Keep online provider disabled until provider event table, idempotency key, signature verification, and replay tests exist. | No for manual-only staging; Yes before provider UAT | Conditional |
| R-013 | Production-like runtime is intentionally fail-closed due lower-env providers/stubs | backend fix | blocker | Runtime safety treats lower-env fixtures, storage stubs, static contacts, payment stubs, communications stubs, and map/certificate/export shells as production-like blockers (`apps/api/src/runtime-safety.ts:10-58`, `98-121`). | The backend cannot currently start under true `API_RUNTIME_MODE=staging/production` until these are replaced/configured or deliberately staged behind approved feature flags. | Replace lower-env providers with configured staging adapters or add explicit staging-safe disabled adapters with no fake production data. | Yes | No |
| R-014 | No GitHub CI workflow enforcing DB integration was found | test fix | high | `db:integration:test` exists (`package.json:15`), and pass reports record manual successful runs, but no `.github` workflow files were present in this checkout. | DB-first closure depends on PostgreSQL integration tests. Without CI enforcement, regressions can reintroduce Map/flush or audit gaps. | Add CI that provisions PostgreSQL, applies migrations, runs `db:integration:test`, and runs lint/typecheck/test/contracts/openapi/migrate/seed checks. | Yes | No |
| R-015 | Migration checks are convention checks, not apply/rehearsal checks | test fix | medium | Reports show `db:migrate:check` passing; migration/seed scripts check conventions and lower-env seed flags (`packages/db/scripts/check-seeds.mjs:26-37`), while production readiness checklist still requires migration rehearsal (`docs/implementation/production-readiness-checklist.md:35-43`). | AWS staging/UAT needs proof migrations apply cleanly to PostgreSQL, not only naming/convention success. | Add a clean-database migration apply/check job and record rollback/import rehearsal separately. | Yes | No |
| R-016 | Lower-env seed/fixture blocking in production-like runtime needs executable proof | test fix | medium | Runtime safety blocks production-like lower-env issues (`apps/api/src/runtime-safety.ts:77-121`); Pass reports record `db:seed:check`, but no current checkpoint run proved startup behavior under all intended AWS env combinations. | A staging config mistake could accidentally enable fake contacts/storage/payment/communication stubs. | Add production-like startup tests for `NODE_ENV=production`, `API_RUNTIME_MODE=staging`, and `API_RUNTIME_MODE=production`, with and without required provider config. | Yes | No |
| R-017 | Official scoring/subcriteria/guidance and applicant bands are external | external launch gate | blocker | Gap register lists official scoring and applicant bands as external (`docs/implementation/gap-register.md:8-10`); result DTO avoids applicant bands in safe projection. | Engineering must not invent scoring text, subcriteria, guidance, thresholds, or applicant score bands. | Obtain KBT-approved scoring/band configuration and load it as versioned config/seed after approval. | Conditional for technical staging; Yes before scoring UAT/signoff | No |
| R-018 | Finance, provider, Business Central, storage/scanning, public map, and KBT signoff are external gates | external launch gate | blocker | Gap register lists fee/VAT/legal, Business Central, payment provider, SMS, public map contract, migration/governance gates (`docs/implementation/gap-register.md:10-20`); system state says real storage/scanning/payment/communications/public-map dispatch are not implemented (`docs/implementation/system_state.md:82-86`). | These are launch dependencies, not coding assumptions. | Track as launch/UAT gates; do not hardcode production fees, legal wording, credentials, or provider payloads. | Conditional by staging scope | No |

## 3A. Closeout Pass B residual ledger update

Verdict: CLOSED - PASS WITH CAVEATS for the Pass B scope.

Closed by Closeout Pass B:

- R-009 upload session idempotency collision/replay path is closed. Same-key replay, concurrent replay, and conflicting metadata behavior are covered by the PostgreSQL repository path and integration tests.
- R-010 result publish side-effect idempotency is closed for current publish semantics. Artifact and public-map event side effects use nullable `dedupe_key` values and narrow partial unique indexes, avoiding broad uniqueness constraints that would block future republish/revision/history contracts.
- R-011 notification/job/export retry duplication is closed within scope. Renewal reminder job replay and explicit-idempotency notification replay are handled; export replay dedupes only when an idempotency key is present.
- R-014 GitHub CI enforcement gap is closed locally. A workflow now provisions PostgreSQL/PostGIS and runs the expected check families.
- R-015 clean migration apply gap is closed locally. The check requires disposable-test opt-in, refuses production/staging/shared markers, applies discovered migrations on a disposable database, and fails with create/drop guidance.
- R-016 production-like runtime proof is closed for guard behavior. Tests cover `NODE_ENV=production`, `API_RUNTIME_MODE=staging`, and `API_RUNTIME_MODE=production` fail-closed behavior around lower-env providers and mutable store fallbacks.

Preserved caveats:

- Renewal reminder notification-level dedupe is only target-safe with an explicit idempotency key until a real certificate/park/episode/recipient/offset/expiry target model exists.
- CI was added and locally inspected/tested, but no remote GitHub Actions run exists from this local session.
- Provider-backed actions remain disabled until real staging/provider configuration is supplied; R-013 and R-018 remain launch/staging-provider caveats rather than Pass B blockers.

## 4. Recommended hardening sequence

1. Security/DTO hardening pass: split applicant/org/public DTOs from admin/internal DTOs for documents, messages, assessment evidence, result artifacts, exports, and certificate availability. Add forbidden-field tests for `storageProvider`, `storageKey`, raw scores, internal notes, judge identities, visit dates, assignment state, Mystery metadata, hidden thread metadata, and suppressed notification details.
2. RBAC/query-scope pass: replace primary-role-plus-all-scopes authorization with role-assignment tuple checks where needed; add resource-scoped admin communications/jobs/exports queries; add cross-country/cross-organisation/cross-park denial tests and read-only viewer mutation denial tests.
3. Idempotency/retry pass: harden upload-session replay, result publish side effects, public-map event uniqueness, renewal reminder/export duplicate suppression, and manual/provider payment replay boundaries.
4. Production-like runtime pass: replace or explicitly disable lower-env providers for staging config, add production-like startup tests for `NODE_ENV=production`, `API_RUNTIME_MODE=staging`, and `API_RUNTIME_MODE=production`, and keep provider-backed actions disabled until credentials/contracts exist.
5. CI/migration readiness pass: add GitHub CI with PostgreSQL service, migration apply, `db:integration:test`, and existing lint/typecheck/unit/contracts/openapi/migrate/seed checks.
6. External UAT enablement pass: load only approved scoring, band, finance, provider, Business Central, S3/scanning, public map, and notification/template configuration; record KBT UAT/signoff evidence.

## 5. Explicit external gates

Engineering must not invent:

- Official Standard Green Flag criteria, subcriteria, threshold guidance, scoring guidance, or applicant-facing result wording.
- Applicant bands, band labels, or band ranges.
- Fee schedule, VAT treatment, legal invoice wording, or invoice/certificate legal text.
- Payment provider credentials, webhook signatures, event payloads, or online card-flow behavior.
- Business Central data contract, credentials, API mapping, or automated finance integration.
- S3 bucket policy, signed URL provider, virus scanning provider, quarantine/reject rules, or production object-key conventions.
- SMS/email provider accounts, credentials, sender identities, template copy, suppression policy approval, or provider retry/dead-letter settings.
- Public map adapter endpoint, payload contract beyond current provider-neutral event shell, retry policy, or publication SLA.
- KBT UAT approval, legal/compliance approval, go-live signoff, monitoring/alerting thresholds, backup/rollback acceptance, or migration import signoff.

## 6. Mutation check

Initial `git status --short` before this checkpoint was already dirty, including modified backend files, modified implementation docs, untracked Pass 1/2 reports, untracked PostgreSQL communications/results repositories, untracked migration `0019_communications_db_first_safety.sql`, source schema docs, and `output/`.

After writing this report, `git status --short` showed the same pre-existing dirty tree plus this new untracked report:

- `docs/implementation/working/backend-closure-residual-issues-ledger.md`

Confirmed checkpoint mutation scope: only this report file was created by this checkpoint. No source code, migrations, tests, OpenAPI/contracts, package files, lock files, generated clients, fixtures, seed files, configs, snapshots, or destructive commands were intentionally changed by this checkpoint.
