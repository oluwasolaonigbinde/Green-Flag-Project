# S12.5 PostgreSQL Read-Model Normalisation And Runtime Payload Hardening

## Metadata

- Slice ID: 12.5
- Title: PostgreSQL read-model normalisation and runtime payload hardening
- Status: Frozen
- Planned on: 2026-05-06

## Objective

Promote high-value Slices 3-12 read-model fields into relational columns/child tables and indexed query paths while preserving existing DTO hydration.

## Scope

- Keep existing DTO hydration stable during the transition.
- Persist application sections and field values into their existing relational tables.
- Persist upload accepted chunks into `document_upload_chunks`.
- Persist assessor preferences, availability windows, and capacity declarations into their existing relational tables.
- Add and populate normalized assessment score entries so S13/S14 can query scores relationally.
- Add targeted read-model indexes for queue/export/publication paths.
- Add DB integration tests that verify normalized relational rows are written alongside payloads and rollback behavior remains intact.

## Out Of Scope

- Removing the legacy JSON compatibility envelope.
- Changing public DTO shapes.
- Rewriting route-level stores into a new ORM/repository architecture.
- Implementing S13 notifications, messages, jobs, exports, or public map dispatch.
- Inventing official criteria, score bands, certificate wording, or provider contracts.

## Success Criteria

- Existing APIs continue to hydrate from current payload-compatible stores.
- Normalized child tables are populated transactionally with the same domain writes.
- Read-model indexes exist for admin queues, allocation/assessment/result lookups, and S13 export/event candidates.
- Full required checks pass.

## Closure

- Closed on: 2026-05-06
- Status: DONE_FULL
- Delivery record: `docs/implementation/delivery-records/S12.5-postgres-read-model-normalisation-hardening-delivery.md`
