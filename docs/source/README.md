# Source Corpus Notes

The files in this directory are original source artifacts. Prefer not to rewrite these binary/client artifacts during implementation work.

When source documents overlap or appear contradictory, use `docs/implementation/source-reconciliation.md` plus the precedence rules in `docs/implementation/Green_Flag_System_Architecture_Final.docx` to determine implementation truth.

For S09 allocation, the requirements specs and architecture provide implementable default rules for judge-count suggestions, COI handling, rotation, hold/release, contact reveal, candidate scoring, and Mystery suppression. The remaining open allocation items are production configuration/import inputs, not a reason to block the configurable S09 foundation.
